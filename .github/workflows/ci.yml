# PR / main 门禁：lint + test（对齐 .gitlab-ci.yml backend:lint/test、frontend:lint/test）
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: backend/uv.lock

      - name: Ruff
        run: uv sync --group dev && uv run ruff check .

      - name: Pytest
        run: uv run pytest -q -m "not integration and not load"

  godot-builder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Godot builder image
        run: docker build -f docker/Dockerfile.godot-builder -t gameforge-godot-builder:v1 .

      - name: Verify Godot CLI
        run: docker run --rm gameforge-godot-builder:v1 godot --version

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11.21.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm exec tsc -p tsconfig.json --noEmit

      - name: Test
        run: pnpm test
