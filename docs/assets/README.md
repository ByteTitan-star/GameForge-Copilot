# README media checklist

This directory currently contains real product screenshots used by the root README files.

## Pending gameplay walkthrough

Add the following files in a follow-up documentation-only change once the game-generation workflow is ready for recording:

- `gameplay-demo.mp4`: 30-60 second local recording.
- `gameplay-demo-cover.png`: a frame from the recording used as the README link thumbnail.

The recording must show this real product flow:

```text
Describe game -> Confirm design -> Generate game -> Play in browser -> Actual controls
```

Do not show email addresses, API keys, provider configuration, access tokens, or other account details. After adding both files, replace the Gameplay demo text in `README.md` and `README.zh-CN.md` with a linked cover image.
