# 真实 API 前缀（不含末尾斜杠）
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# 可选：托管根（/play、/draft）；默认从 API 去掉 /api/v1
# VITE_HOSTING_BASE_URL=http://127.0.0.1:8000
# 可选：WS 根；默认由 hosting 的 http→ws 推导
# VITE_WS_BASE_URL=ws://127.0.0.1:8000
