# GameForge Frontend

React + Vite + TypeScript + Tailwind。契约见 `../docs/10-contract-and-parallel-dev.md`，视觉见 `../design-system/`，工程约定见 [CONVENTIONS.md](./CONVENTIONS.md)。

```bash
pnpm install
pnpm dev          # http://127.0.0.1:5173
pnpm build
```

默认 `VITE_USE_MOCK=true`。Mock 账号见 CONVENTIONS。
